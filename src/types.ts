export type TVideoDB = {
    id: string;
    title: string;
    duration: number;
    upload_date: string;
  };
  export type TVideoDBPost = {
    id: string;
    title: string;
    duration: number;
  };
  